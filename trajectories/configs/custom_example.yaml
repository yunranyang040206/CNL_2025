# Custom configuration example
# Demonstrates different reward structure

grid:
  size: 5

mdp:
  gamma: 0.95  # Different discount factor
  tau: 0.3     # Sharper policy
  max_iters: 1000
  tol: 1.0e-6

trajectories:
  n_episodes: 100  # Fewer episodes
  t_steps: 200     # Shorter trajectories
  seed: 42

rewards:
  # Multiple state-based rewards
  state_based:
    0: 2.0   # Higher reward at top-left
    12: 1.5  # Reward at center (2,2)
    24: 1.0  # Reward at bottom-right
  
  # No sequence-based rewards in this example
  sequence_based: []

mode_switching:
  base_prob: 0.1    # Higher base switching
  bonus_prob: 0.3   # Lower bonus
  trigger_states:
    12: "water_to_home"  # Center triggers switch
